
// Import standard library
use std::{
	fs::File,
	io::{self, prelude::*},
};

pub struct Reader {
	m_reader: io::BufReader<File>,
}


// Finish custom reader for files (look at links saved in discord)
impl FileReader {
	pub fn 
}